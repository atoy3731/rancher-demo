{{- if .Values.config.loadbalancerEnabled }}
apiVersion: v1
kind: Service
metadata:
  name: rancher-demo-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: demo
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
{{- end }}
